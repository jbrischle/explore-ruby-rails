<h1>Accounts</h1>

<ul>
  <% @accounts.each do |account| %>
    <% unless account.closed? %>
      <li>
        <%= link_to account.title, account %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New Account", new_account_path %>
